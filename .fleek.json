{
	"build": {
		"image": "node:alpine",
		"command": "npm install && npm run build && node ./build && echo $SOME_ENV",
		"publicDir": "build",
		"environment": {
			"SOME_ENV": "Build finished!"
		}
	}
}
