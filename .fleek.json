{
	"build": {
		"image": "node:alpine",
		"command": "npm install && npm run build && node build/index.js && echo $SOME_ENV",
		"publicDir": "build",
		"environment": {
			"SOME_ENV": "Build finished!"
		}
	}
}
